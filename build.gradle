apply plugin: 'java'

jar {
    manifest {
      attributes("Main-Class": "com.atlassian.rgates.missingframe.Main")
    }
    from {
        configurations.runtime.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
